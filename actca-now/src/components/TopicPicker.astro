---
// --- IMPORTS ---
// (No imports needed for this component)

// --- PROPS ---
// (No props - uses Alpine.js for state management from parent scope)
---

<div class="mb-8 flex flex-wrap gap-3 justify-center">
  <button
    type="button"
    @click="activeTopic = 'all'"
    :class="activeTopic === 'all' ? 'bg-yellow-400 text-slate-950 font-bold' : 'bg-slate-800 text-slate-300 hover:bg-slate-700'"
    class="px-6 py-3 rounded-lg font-semibold transition-all duration-200 min-h-[48px] border-2 border-transparent hover:border-yellow-400/50"
  >
    All Topics
  </button>
  
  <button
    type="button"
    @click="activeTopic = 'rail'"
    :class="activeTopic === 'rail' ? 'bg-yellow-400 text-slate-950 font-bold' : 'bg-slate-800 text-slate-300 hover:bg-slate-700'"
    class="px-6 py-3 rounded-lg font-semibold transition-all duration-200 min-h-[48px] border-2 border-transparent hover:border-yellow-400/50"
  >
    Rail
  </button>
  
  <button
    type="button"
    @click="activeTopic = 'housing'"
    :class="activeTopic === 'housing' ? 'bg-yellow-400 text-slate-950 font-bold' : 'bg-slate-800 text-slate-300 hover:bg-slate-700'"
    class="px-6 py-3 rounded-lg font-semibold transition-all duration-200 min-h-[48px] border-2 border-transparent hover:border-yellow-400/50"
  >
    Housing
  </button>
  
  <button
    type="button"
    @click="activeTopic = 'ai'"
    :class="activeTopic === 'ai' ? 'bg-yellow-400 text-slate-950 font-bold' : 'bg-slate-800 text-slate-300 hover:bg-slate-700'"
    class="px-6 py-3 rounded-lg font-semibold transition-all duration-200 min-h-[48px] border-2 border-transparent hover:border-yellow-400/50"
  >
    AI
  </button>
  
  <button
    type="button"
    @click="activeTopic = 'health'"
    :class="activeTopic === 'health' ? 'bg-yellow-400 text-slate-950 font-bold' : 'bg-slate-800 text-slate-300 hover:bg-slate-700'"
    class="px-6 py-3 rounded-lg font-semibold transition-all duration-200 min-h-[48px] border-2 border-transparent hover:border-yellow-400/50"
  >
    Health
  </button>
  
  <button
    type="button"
    @click="activeTopic = 'rights'"
    :class="activeTopic === 'rights' ? 'bg-yellow-400 text-slate-950 font-bold' : 'bg-slate-800 text-slate-300 hover:bg-slate-700'"
    class="px-6 py-3 rounded-lg font-semibold transition-all duration-200 min-h-[48px] border-2 border-transparent hover:border-yellow-400/50"
  >
    Rights
  </button>
  
  <button
    type="button"
    @click="activeTopic = 'econ'"
    :class="activeTopic === 'econ' ? 'bg-yellow-400 text-slate-950 font-bold' : 'bg-slate-800 text-slate-300 hover:bg-slate-700'"
    class="px-6 py-3 rounded-lg font-semibold transition-all duration-200 min-h-[48px] border-2 border-transparent hover:border-yellow-400/50"
  >
    Economy
  </button>
</div>

